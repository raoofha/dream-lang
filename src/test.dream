hi = "hello world"
console.log hi
#console.log String hello   String   world  String goodbye world
console.log String hello
  String world
  String goodbye world
console.log 2 + String asfzxv

import Html.tags.*

name = "world"

main =
  h1 "hello " name
  div f.name for f in #:/
  (#:/).forEach fn (f) div f.name
  .forEach #:/ fn (f) div f.name
  forEach #:/ fn (f) div f.name
